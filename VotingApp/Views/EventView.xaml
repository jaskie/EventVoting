<UserControl x:Class="EventVoting.VotingApp.Views.EventView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vm="clr-namespace:EventVoting.VotingApp.ViewModels"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance vm:EventViewModel}"
             d:DesignHeight="800" d:DesignWidth="1000">
    <DockPanel>
        <ToolBar DockPanel.Dock="Top" Height="32" >
            <ToolBarPanel Orientation="Horizontal">
                <Button x:Name="NewVoting" Content="{StaticResource VotingNewIcon}" ToolTip="Add new voting"/>
                <Separator Style="{StaticResource {x:Static ToolBar.SeparatorStyleKey}}" />
                <Button x:Name="StartVoting" Content="{StaticResource VotingStartIcon}" ToolTip="Start collecting votes for selected voting"/>
                <Button x:Name="StopVoting" Content="{StaticResource VotingStopIcon}" ToolTip="Finish collecting votes"/>
            </ToolBarPanel>
        </ToolBar>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <DataGrid ItemsSource="{Binding Votings}" SelectedItem="{Binding SelectedVoting}" CanUserAddRows="False" CanUserDeleteRows="False" AutoGenerateColumns="False">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Title" Binding="{Binding Name}" Width="*"/>
                    <DataGridTextColumn Header="Started" Binding="{Binding Start}"/>
                    <DataGridTextColumn Header="Finished" Binding="{Binding End}"/>
                </DataGrid.Columns>
            </DataGrid>           
        </Grid>        
    </DockPanel>
</UserControl>

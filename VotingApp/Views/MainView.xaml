<Window x:Class="EventVoting.VotingApp.Views.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        >
    <DockPanel>
        <Menu DockPanel.Dock="Top" IsMainMenu="True" >
            <MenuItem Header="Event">
                <MenuItem x:Name="EventCreate" Header="Create new" Icon="{StaticResource EventNewIcon}"/>
                <MenuItem x:Name="EventSelect"  Header="Select existing" Icon="{StaticResource EventOpenIcon}"/>
                <MenuItem x:Name="EventClose"  Header="Close event" Icon="{StaticResource EventCloseIcon}"/>
            </MenuItem>
        </Menu>
        <ContentControl x:Name="SelectedEvent"/>
    </DockPanel>
</Window>

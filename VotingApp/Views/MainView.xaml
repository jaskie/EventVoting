<Window x:Class="EventVoting.VotingApp.Views.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Voting controller"
        Height="350" Width="525">
    <DockPanel>
        <Menu DockPanel.Dock="Top" IsMainMenu="True" >
            <MenuItem Header="Event">
                <MenuItem x:Name="EventCreate" Header="Create new" Icon="{StaticResource NewIcon}"/>
                <MenuItem x:Name="EventSelect"  Header="Select existing" Icon="{StaticResource OpenIcon}"/>
                <MenuItem x:Name="EventClose"  Header="Close event" Icon="{StaticResource CloseIcon}"/>
            </MenuItem>
        </Menu>
        <Grid>

        </Grid>
    </DockPanel>
</Window>
